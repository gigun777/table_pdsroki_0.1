{
  "name": "@dilovodstvo/subrows",
  "version": "0.1.0",
  "type": "module",
  "description": "Pure logical subrows model for table engines",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --out-dir dist",
    "test": "vitest run"
  },
  "devDependencies": {
    "tsup": "^8.0.2",
    "typescript": "^5.4.5",
    "vitest": "^2.0.5"
  }
}
